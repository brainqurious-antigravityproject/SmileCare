export const metadata = {
    title: "Login | SmileCare",
    description: "Sign in to your SmileCare account.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
